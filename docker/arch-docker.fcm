# Architecture file for Docker (Debian bookworm + gfortran + OpenMPI)
# No XIOS — uses IOIPSL for output

%NCDF_INC            -I/usr/include
%NCDF_LIB            -L/usr/lib/x86_64-linux-gnu -lnetcdff -lnetcdf
%XIOS_INC
%XIOS_LIB

%OASIS_INC
%OASIS_LIB

%CPP                 cpp -Dkey_nosignedzero
%FC                  mpif90 -c -cpp
%FCFLAGS             -fdefault-real-8 -O3 -funroll-all-loops -fcray-pointer -ffree-line-length-none
%FFLAGS              %FCFLAGS
%LD                  mpif90
%LDFLAGS
%FPPFLAGS            -P -traditional
%AR                  ar
%ARFLAGS             rs
%MK                  make
%USER_INC            %NCDF_INC
%USER_LIB            %NCDF_LIB

%CC                  cc
%CFLAGS              -O0
